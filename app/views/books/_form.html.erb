<%= form_with(model: @book, local: true) do |form| %>

<div class = "form">
  <p><%= form.label "Name: " %></p>
  <%= form.text_field :name %>
</div>

<div class = "form">
  <%= form.submit "Submit" %>
</div>

<% end %>
