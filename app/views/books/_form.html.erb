<%= form_with(model: @book, local: true) do |form| %>

<div>
  <h3>
    <%= form.label "Name: " %>
    <%= form.text_field :name %>
  </h3>
</div>

<div>
  <h3>
    <%= form.submit "Submit", :class => "button btn btn-default btn-lg" %>
  </h3>
</div>

<% end %>
